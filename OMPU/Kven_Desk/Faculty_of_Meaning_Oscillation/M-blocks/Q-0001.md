# Q-0001 — moved

Moved to `/SIGNAL_IN/Q-0001.json` (canonical copy).

This block previously lived here as a second copy of the same M-block. To
avoid edit-drift, the canonical version now lives in `SIGNAL_IN/` and this
location keeps only this pointer.
